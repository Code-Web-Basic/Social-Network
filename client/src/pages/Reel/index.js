import { Stack, Typography } from '@mui/material';

function Reels() {
    return (
        <Stack width="100%" height="10px" justifyContent="center" padding="10px" alignContent="center">
            <Typography variant="h6">Features in development</Typography>
        </Stack>
    );
}

export default Reels;
